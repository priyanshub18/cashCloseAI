"""Deterministic synthetic datasets for CashClose demos and evaluations.

Import the generator from :mod:`packages.synthetic_data.generator`. Keeping the
package initializer side-effect free also makes ``python -m`` execution clean.
"""
